[project]
name = "deep-learning-from-scratch-4"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["numpy>=2", "matplotlib>=3", "torch", "gymnasium[classic-control]"]

[tool.ty.src]
exclude = ["lib"]
